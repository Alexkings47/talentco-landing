import React from "react";
import styled from "styled-components";

const Application = () => {
  return <StyledSection>Application</StyledSection>;
};

export default Application;
const StyledSection = styled.section``;
