import React from "react";
import styled from "styled-components";

const Testimonial = () => {
  return <StyledSection>Navbar</StyledSection>;
};

export default Navbar;
const StyledSection = styled.section``;
